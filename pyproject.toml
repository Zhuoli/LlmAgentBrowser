[project]
name = "llm-browser-agent-demo"
version = "0.1.0"
description = "Demo using agent-browser for LLM-powered browser automation to fetch tweets"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.0.0",
    # LLM Provider SDKs
    "anthropic>=0.40.0",
    "openai>=1.50.0",
    "google-genai>=1.0.0",
]
